# linux 下的编译

> 本文档描述 linux 下的 IHttp 项目的编译，和遇到的问题

> TODO: 这个我也不熟，但我编译下来了，也在线运行了，用户神通广大，比我强，肯定没问题。

这里我遇到一个问题，就是用户如果 安装了Qt5.14.2的 离线Qt, 如果在命令行安装，就不行，必须命令行编译的才可以。
Qt 版本冲突的问题。我没有在深究，如果有人遇到这个问题，可以写一个详细的描述文档。

如下是我的编译方式:

```
sudo apt install qtbase5-dev
qmake
make -j20
```

注意 Qt 版本的问题。